var connection = require("./connection.js");

//Create the ORM methods that will execute the mysql commands in the controllers  
var orm = {
    selectAll =();
    insertOne = ();
    updateOne = ();
}

module.exports(orm);